# CarRacing-v2 PPO — Requirements
# Install with: pip install -r requirements.txt

# Core RL stack
gymnasium==0.29.1                 # Environment API
Box2D==2.3.10                     # Prebuilt Box2D physics bindings for CarRacing on Windows
pygame>=2.1.3                     # Rendering dependency used by Gymnasium CarRacing
stable-baselines3==2.3.2          # PPO, evaluation utils, VecEnvs
torch>=2.0.0                       # Neural network backend (CPU or CUDA)

# Visualisation & monitoring
tensorboard>=2.14.0                # Training curves
opencv-python>=4.8.0               # Video recording (RecordVideo wrapper)

# Utilities
numpy>=1.24.0
tqdm>=4.66.0                       # Progress bars

# Optional: GPU support
# If you have an NVIDIA GPU, install PyTorch with CUDA instead:
# pip install torch --index-url https://download.pytorch.org/whl/cu118
